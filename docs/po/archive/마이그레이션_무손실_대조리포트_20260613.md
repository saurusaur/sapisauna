# 구→신 로그 스키마 마이그레이션 무손실 대조 리포트

2026-06-13 / 대상: 라이브 DB (logs 364 · deep_logs 337 · log_blocks 865) / 030 cleanup 직전 검증

## 방법 (이중 검증)

1. **기계 전수 대조** — `scripts/audit-migration-dump.mjs`(service role, curl)로 전체 덤프 → 사우나명 순 8버킷 → `scripts/audit-migration-check.mjs`가 규칙 R1~R8 전수 검사 → **0건**
2. **블라인드 팬아웃** — 에이전트 8개가 버킷별로 검증 스크립트를 **각자 독립 작성**해 전수 재검사 + 표본 107건 raw JSON 눈검증(빈문자열 vs null, 단위 환산, 통화, 0값 보존까지)

검사 규칙: R1 컬럼 개명 3종 / R2 deep 흡수(5필드+열탕·급냉) / R3 세신 3분기 / R4 매점→snack / R5 food_eaten / R6 유저 블록 보존(온도10·heat/ice/rest 시간 합·평가) / R7 어드민=전부 is_extra / R8 is_extra 규칙

## 결론: **데이터 손실(LOSS) 0건** ✅

| 판정 | 건수 | 내용 |
|---|---|---|
| LOSS | **0** | 구 데이터가 신 스키마 어디에도 없는 케이스 없음 |
| MISMATCH/RULE | 3 | heat_time 귀속 위치 차이 (아래 — 시간 자체는 보존) |
| INFO | 11 | 설계대로 동작 확인 (아래) |

## 귀속 차이 3건 (교정 SQL: `supabase/034_heat_routing_fix.sql`)

원인: 029 원본이 `primary_sauna_kind`(기본 dry)를 트라이브보다 우선해 heat_time을 건식 블록에 귀속. 합계는 정확, 의미만 변질(찜질·목욕 시간이 "건식 N분"으로 표시).

| 장소 | 트라이브 | 현재 | 교정 |
|---|---|---|---|
| 아트리파라다이스 | jimi | 건식 900s | → 한증막 |
| 죽전누리사우나 | jimi | 건식 720s | → 한증막 |
| 킹스호텔사우나 | bather | 건식 720s | → 온탕 |

## INFO 11건 (조치 불요 또는 확인 1건)

- **열탕 flag=false 6건** (강남24시 44° / 그랜드조선 부산 43° / 제주 41° / 그랜드워커힐 42° / 기린온천 44° / 대영온천 44°): 구 deep의 `has_very_hot_bath=false`인데 온도값만 잔존 → **설계대로 미이관**(체크 해제=의도적 미기록으로 간주). 전부 어드민 시드 — 살릴지 유저 결정 대기.
- food_eaten: 전 로그 빈 배열 — 폐기해도 실질 손실 0 확정.
- 빈문자열 memo(`""`) → null 변환 없이 그대로 보존 확인 (3건 눈검증).
- scrub variant 표현차 2건: `scrub_types:['scrub']` 유래는 variant=null, `[]` 유래는 'basic' — 코드가 null≡basic 동등 취급이라 무해 (034에서 'basic'으로 정규화).
- 0값 보존: ice_bath_temp=0, cost=0 모두 유지. 900,000원 cost 아웃라이어도 원본 그대로(입력 실수 여부는 별건).

## 산출물

- 덤프·체커: `scripts/audit-migration-dump.mjs` · `scripts/audit-migration-check.mjs` (재실행 가능)
- 원자료: `tmp/migration-audit/` (로컬 전용, 커밋 제외)
- 워크플로: 8 agents · 36.6만 토큰 · 13분

## 030 진행 판정

**GO** — 손실 0 확인. 단 순서: ① 034 교정(선택이지만 권장) → ② 코드 폴백 제거 커밋 main 배포(LOG_SELECT의 deep_logs 조인 제거 — 이거 없이 030 돌리면 읽기 전부 깨짐) → ③ 030 실행.
