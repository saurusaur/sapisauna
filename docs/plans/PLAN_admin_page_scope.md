# PLAN: 어드민 페이지 구성 스코핑 (L4) — 2026-06-04

> 목적: `/admin` 신규 페이지의 **기능·화면·권한** 정의 (헤드라인 + MVP 우선순위). 구현 전 스코핑 단계.
> 연계: BACKLOG "어드민 페이지 구축(P1)", `docs/po/UX_DIRECTION_page_emphasis_20260604.md` L4.

---

## 1. 배경 / 현황
- `/admin` 라우트 **없음**(신규 구축 대상).
- 현재 어드민 동작 방식:
  - **장소 등록·교정** = `scripts/katalk-*` 스크립트로만(비개발 운영 불가).
  - **큐레이션(is_featured)** = `toggle_featured` RPC(마이그레이션 012), sa-list 상세에서 어드민만 토글. 어드민 식별 = **`ADMIN_USER_ID` 하드코딩**(`src/constants/content.ts`).
  - **어드민 온도 로그** = `ADMIN_USER_ID`로 작성, 유저 노출 제외(`logs-service.ts`).
- 문제: 큐레이션·등록·교정·신고처리용 **운영 UI 부재** → P0 큐레이션 시드·F6 운영의 병목.

## 2. 권한 모델 (⚠️ 결정 필요)
- 현재: 단일 `ADMIN_USER_ID` 하드코딩(1인).
- 옵션: **(a) 하드코딩 ID 유지**(빠름, 솔로 운영) / (b) `profiles.role` 컬럼 추가(확장·다인) / (c) env allowlist.
- 게이트: `middleware.ts`에서 `/admin` 보호(현재 public: /login·/explore·/api) + **서버에서 재검증**.

## 3. 기능 모듈 (화면 구성)
| # | 모듈 | 내용 |
|---|---|---|
| 1 | 대시보드 | KPI(유저·로그·장소 수, 트라이브 분포, 최근 활동·신규 가입) |
| 2 | 장소 관리 | 검색/목록, **수동 등록**, 편집(facility_type·bath_policy·city·facilities·24h), 병합, 폐업 처리 |
| 3 | 데이터 교정 | place_sources(이름·주소·source·external_id)·좌표·**어드민 온도 로그** 교정 |
| 4 | 큐레이션 | is_featured SA-리스트 관리(추천 등록/해제·노출 순서), FEATURED 콜드공백 관리 |
| 5 | 검수 큐 | 유저 신고("다른 장소"/"폐업") 처리, 수동 등록 리뷰 큐 |
| 6 | (확장) 품질 리포트 | 데이터 품질 플래그(`katalk-db-full-audit.mjs` 연동), 온도 이상치, resort-spa 재분류 후보 |
| 7 | **피드백·문의 인박스** | 유저 피드백 + 협업/제휴 제안 **수신·관리**(읽음·상태·답변 메모). ⚠️유저측 **제출 진입점**(앱 내 문의/피드백 폼) 선행 필요 → 신규 `feedback` 테이블 |
| 8 | **문구·용어 관리(CMS)** | 앱 카피(문구) + **용어 사전(GLOSSARY)** + 라벨 편집. ⚠️현재 `src/constants/content.ts`에 코드로 존재 → CMS화 시 DB 이전 결정 필요(§6.5). "뉴비 용어 팝업(BACKLOG P1)"의 단일 소스 |
| 9 | **디자인 에셋 관리** | 이미지·아이콘·트라이브 그래픽·스토리 템플릿 에셋 등록/교체(Supabase Storage). 버전·노출 관리 |
| 10 | **컬러/테마 설정** | 브랜드/트라이브/스토리 컬러 토큰·hue 관리. ⚠️현재 `story-colors.ts`·hue 시스템(코드) → DB 토큰화 vs 코드 유지 결정(§6.5) |
| 11 | **앱 로고/브랜딩 설정** | 인앱 로고 + 파비콘 + PWA 아이콘(매니페스트)·스플래시 + OG 이미지 교체/버전 관리. ⚠️PWA manifest·favicon은 빌드 자산이라 런타임 교체 범위 결정 필요(§6.5) |

## 4. MVP 우선순위
- **P0 (베타 운영 필수)**: ④큐레이션 관리 + ②장소 등록/편집 + ③교정 → 큐레이션 시드·직접 운영 가능.
- **P1**: ⑦피드백·문의 인박스(+유저 제출 폼), ⑤검수 큐, ①대시보드.
- **P2**: ⑧문구·용어 관리, ⑩컬러/테마, ⑪로고/브랜딩, ②병합/폐업 자동화, ⑥품질 리포트.
- **P3**: ⑨디자인 에셋 관리(풀 CMS).
> ⑧⑩⑪(콘텐츠·컬러·로고)은 **§6.5 "코드 상수 vs DB CMS화" 결정 선행** — 결정 전엔 스코프만.

## 5. 기술 메모
- 라우트 `/admin/*`(App Router) + 미들웨어 가드 + 서버 재검증.
- 쓰기 다수가 **service_role 필요** → **서버 route handler**로 처리(클라에서 service key 직접 사용 금지).
- 기존 스크립트 로직(**등록 컨벤션·온도 범위검증·좌표 프록시미티 중복가드**, `katalk-new-register`/`katalk-overseas-register`)을 서버 API로 이식·재사용.
- 데이터 모델은 **L1 로그·스키마 재검토 결과에 영향받음**(③ 온도 교정 화면) → ③·⑥은 L1 이후 확정.
- **에셋/로고(⑨⑪)**: Supabase Storage 버킷 + 메타 테이블, 런타임 참조. PWA manifest·favicon은 빌드 자산이라 런타임 교체 가능 범위 확인 필요.
- **콘텐츠/컬러(⑧⑩)**: 코드 상수 유지 시 어드민은 "편집 제안→PR/배포" 흐름, DB 이전 시 런타임 fetch + 캐시. 이중관리·캐시 무효화 트레이드오프.

## 6. 미결정 (유저 확인 필요)
1. **권한 방식**: 하드코딩 ID 유지 vs `profiles.role` 신설(향후 운영자 추가 계획?).
2. **등록 UI 범위**: 대량=스크립트 유지 / 단건·교정=UI? 어디까지 UI화?
3. **큐레이션 소유 모델**: 어드민 계정 소유 리스트 vs featured 플래그만(현행).
4. **검수 큐 신고 입력 경로**: 유저 신고 UI(장소 상세 "신고") 선행 필요 — 별도 백로그 연계.
5. **콘텐츠·컬러·로고(⑧⑩⑪) 관리 아키텍처 (핵심)**: 현재 코드 상수(`content.ts`·`story-colors.ts`·빌드 자산) → **(a) 코드 유지**(어드민=편집 가이드/제안, 반영은 배포) vs **(b) DB CMS화**(런타임 fetch+캐시, 즉시 반영). 범위·우선순위 결정 필요.
6. **피드백·문의(⑦)**: 수신 채널 = 앱 내 폼만 vs 이메일(sapi.sauna@gmail.com) 연동 vs 둘 다. 협업/제휴는 별도 카테고리로 분리?

## 7. 다음 액션
- 위 §2·§6 결정 → MVP(④+②+③ 일부) 화면 와이어 → 서버 API 설계 → 구현 위임.
